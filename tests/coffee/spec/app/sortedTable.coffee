define [
    "when"
], (WhenP) ->

    # console.log "S"

    # describe "an async spec - sorted table", ->
    #     async = new AsyncSpec(@)
  
    #     async.it "load sorted table", (done) ->    
    #         WhenP(moduleHash.getModuleAsPromise("sortedTable")).then (target) ->
    #             console.log "sortedTable", target
    #             expect(target).toBeDefined()
    #             done()
            